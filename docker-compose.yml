version: '3'
services:
  logexp-web:
    image: logexp-img
    build: ./
    depends_on:
      - logexp-db
    ports:
      - "7000:5000"

  logexp-db:
    image: postgres
    environment:
      POSTGRES_DB: logexp_dev
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - db-data:/var/lib/postgresql/
    restart: unless-stopped

volumes:
  db-data:

